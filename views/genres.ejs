<div class="genres-page">
  <% if (locals.showAddGenreDialog) { %> <%- include ('partials/genresModal') %>
  <% } %> <% if (locals.showDeleteDialog) { %> <%- include
  ('partials/deleteModal') %> <% } %>
  <div class="genres-header">
    <h1 class="genres-page-title">Genres Page</h1>
    <a href="/genres/new"
      ><button class="add-entry-button">Add Genre</button></a
    >
  </div>
  <div class="genres-container">
    <% genres.map(genre => {%>
    <div class="genre-wrapper">
      <a href="/books?genres=<%= genre.name %>" class="genre"
        ><%= genre.name %></a
      >
      <div class="action-button-container">
        <a href="/genres/<%= genre.id %>/edit"
          ><i class="fas fa-edit action-icon fa-xl"></i
        ></a>

        <% if (!usedGenres.has(genre.name)) { %>
        <a href="/genres/<%= genre.id %>/delete"
          ><i class="fas fa-trash action-icon fa-xl"></i
        ></a>
        <% } %>
      </div>
    </div>
    <% }) %>
  </div>
</div>
